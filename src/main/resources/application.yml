server:
  port: 8080

spring:
  application:
    name: pizza-api
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://roundhouse.proxy.rlwy.net:24382/railway
    username: postgres
    password: kcaarFDkZeHSlrmZwnKrvZVCqRCpErhm
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format-sql: true
